feat(ci): optimizacion de pipeline de compilacion C++ y opcion de compilacion limpia con cache configurable en GitHub Actions

- Pipeline CI/CD GitHub Actions (.github/workflows/build-apk.yml):
  * Selector interactivo de caché en workflow_dispatch (enable_cache: boolean, activado por defecto).
  * Soporte para compilación limpia (Clean Build): omisión y purga de cachés de CCache, CMake/Ninja, crates de Rust y Gradle (--no-build-cache / cache-disabled).
  * Instalación de herramientas nativas C++ de Linux: Ninja build, CCache, Clang, LLVM, LLD y Build-Essential.
  * Configuración automatizada de Android NDK (28.2.13676358), CMake (3.22.1) y Build Tools (36.0.0).
  * Detección y exportación de variables de entorno ANDROID_NDK_HOME y ANDROID_NDK_ROOT para interoperabilidad con CMake y cargo-ndk.
  * Caché multicapa condicional para artefactos C++ (~/.ccache, app/.cxx, build/intermediates/cxx, cmake).

- Muestreador de Logits C++ (app/src/main/cpp/sampler.h):
  * Implementación de penalización de repetición (Repeat Penalty) sobre historial reciente.
  * Normalización con Temperatura y distribución probabilística Softmax.
  * Filtros combinados Top-K y Top-P (Nucleus Sampling) con generación pseudoaleatoria std::mt19937.

- Bucle Autorregresivo y Streaming JNI (app/src/main/cpp/local_ai_engine.cpp y NativeCppBridge.kt):
  * Implementación de generateStreamingPromptNative con callback JNI reactivo (NativeTokenCallback).
  * Emisión continua de piezas de texto decodificadas directamente a la UI de Kotlin/Compose.
  * Detección de token de parada EOS y parada en caliente (cancelGgufInference).

- Sincronización de Documentación:
  * Actualizados README.md, ROADMAP.md, STRUCTURE.md y AI_CONTEXT.md reflejando la finalización de la Fase 2.7 y optimizaciones de CI.
