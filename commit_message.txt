feat(tflite): preparar terreno con dependencias tflite e integracion inicial de formato

- Configuración de Dependencias (`gradle/libs.versions.toml` y `app/build.gradle.kts`):
  * Adición de las librerías oficiales de TensorFlow Lite: `tensorflow-lite` (2.16.1), `tensorflow-lite-gpu` (2.16.1) y `tensorflow-lite-support` (0.4.4).
  * Preparación del entorno de compilación para soporte de aceleradores móviles (GPU Delegate y NNAPI Delegate).

- Modelo de Datos y Enums (`app/src/main/java/com/example/model/`):
  * Incorporación de `ModelFormatType.TFLITE` (`.tflite`) con descripción técnica clara sobre tensores FlatBuffers y tokenizadores complementarios.
  * Añadido `InferenceBackend.TFLITE_RUNTIME` ("TensorFlow Lite / LiteRT") con soporte de GPU (OpenCL/OpenGL) y NNAPI NPU.

- Persistencia Local (`ModelEntity.kt`):
  * Soporte bidireccional para el formato `"TFLITE"` en la capa de persistencia Room.

- Interfaz de Usuario y Diálogos (`app/src/main/java/com/example/ui/dialogs/`):
  * `ImportModelDialog.kt`: Selector de formato TFLite, detección automática de archivos `.tflite` y `.task`, y campos para archivos opcionales de tokenizador (`tokenizer.json` / `tokenizer.model` / `vocab.txt`) y configuración (`config.json`).
  * `TokenizerGuideDialog.kt`: Actualizada la guía visual explicativa con la comparativa GGUF vs SafeTensors vs TensorFlow Lite.
  * `ParametersDialog.kt`: Selección y restauración adaptativa del backend TFLite Runtime.

- ViewModel y Motor de Inferencia (`ChatViewModel.kt`, `LocalInferenceEngine.kt`):
  * Asignación automática de backend TFLite al importar o seleccionar modelos `.tflite`.
  * Respuestas y documentación integrada sobre compatibilidad con FlatBuffers y delegados móviles.

- Pruebas Unitarias (`app/src/test/java/com/example/ExampleUnitTest.kt`):
  * Prueba unitaria `testTFLiteModel_EntityAndFormatMapping` verificando el mapeo de entidad y propiedades del formato TFLite.

- Documentación Técnica (`README.md`, `AI_CONTEXT.md`, `ROADMAP.md`, `STRUCTURE.md`):
  * Actualización integral de la documentación del proyecto reflejando la preparación del ecosistema TensorFlow Lite.
