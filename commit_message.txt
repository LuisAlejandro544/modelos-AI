feat(engine): implementacion de motor nativo Hugging Face Candle en Rust para inferencia real de SafeTensors

- Integrado Hugging Face Candle 0.8 en app/src/main/rust/ (Cargo.toml y lib.rs):
  * Carga y deserializacion de tensores .safetensors con memoria mapeada (mmap zero-copy).
  * Carga de tokenizador BPE nativo desde tokenizer.json con el crate tokenizers.
  * Muestreo autoregresivo con LogitsProcessor (soporte para temperatura y Top-P).
  * Decodificacion token a token y deteccion de tokens de parada (<|endoftext|>, <|im_end|>, <end_of_turn>, <|eot_id|>).
- Conectado RustInferenceBridge y LocalInferenceEngine para ejecutar inferencia real de paquetes SafeTensors.
- Actualizado el workflow de GitHub Actions (.github/workflows/build-apk.yml) con toolchain de Rust (aarch64-linux-android, armeabi-v7a, x86_64) y cargo-ndk para empaquetar liblocal_ai_rust.so en el APK.
